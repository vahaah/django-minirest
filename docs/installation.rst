============
Installation
============

At the command line::

    $ easy_install django-minirest

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv django-minirest
    $ pip install django-minirest
